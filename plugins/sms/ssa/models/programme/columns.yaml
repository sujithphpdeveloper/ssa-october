columns:
    id:
        label: ID
        type: number
    title:
        label: Title
        type: text
    slug:
        label: slug
        type: text
    created_at:
        label: created_at
        type: datetime
    is_published:
        label: Publish
        type: switch
