columns:
    id:
        label: ID
        type: number
    name:
        label: Name
        type: text
    slug:
        label: slug
        type: text
    created_at:
        label: created_at
        type: datetime
    is_published:
        label: Publish
        type: switch
